image: golang:1.8.3

pipelines:
  default:
    - step:
        script:
          - source bitbucket-pipelines-go.sh
          - cd ${IMPORT_PATH}
          - go get -u github.com/golang/lint/golint
          - go version
          - go get
          - lint=$(golint ./... | grep -v ".pb.go") && if [[ ! -z $lint ]]; then echo $lint && exit 1; fi
          - go build
          - go test -covermode=count -coverprofile=test-reports/count.out
          - go test bitbucket.org/ignitionrobotics/ign-go/test -covermode=count -coverprofile=test-reports/count-test.out
          - go tool cover -html=test-reports/count.out -o test-reports/coverage.html && go tool cover -func=test-reports/count.out
          - go tool cover -html=test-reports/count-test.out -o test-reports/coverage-test.html && go tool cover -func=test-reports/count-test.out
        services:
          - database

definitions:
  services:
    database:
      image: mysql
      environment:
        MYSQL_ROOT_PASSWORD: root
        MYSQL_DATABASE: root
